# Procfile - Tells cloud platforms how to run your app
# Used by Heroku, Railway, and similar platforms

# For API Backend (FastAPI)
web: uvicorn src.backend.main:app --host 0.0.0.0 --port $PORT

# Alternative: For Streamlit Dashboard
# web: streamlit run src/frontend/user_flow_app.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
